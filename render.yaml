# Render Blueprint specification
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: maternal-health-ai
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
      - key: MODEL_PATH
        value: "models/rf.joblib"
